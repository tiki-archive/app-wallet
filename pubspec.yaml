name: tiki_wallet
description: flutter lib for managing crypto keys
version: 0.0.17
homepage: https://mytiki.com
repository: https://github.com/tiki/wallet
issue_tracker: https://github.com/tiki/wallet/issues

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.0.2
  pointycastle: ^3.5.2
  flutter_secure_storage: ^5.0.2
  sqflite_sqlcipher: ^2.1.0
  collection: ^1.15.0
  httpp: ^0.0.4
  tiki_localchain: ^0.0.5
  tiki_kv: ^0.1.1
  tiki_syncchain: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
